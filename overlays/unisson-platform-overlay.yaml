overlay: 1.0.0
info:
  title: API Evangelist enhancements for the Unisson Platform API
  version: 1.0.0
extends: openapi/unisson-openapi-original.json
x-generated: '2026-07-21'
x-method: generated
actions:
- target: $.info
  update:
    description: >-
      Unisson platform REST API (api.unisson.ai) covering agents, triggers,
      evals, chat, runs, scheduled runs, skills, knowledge base, products,
      integrations, customers, meetings, memory, emails, the external Runner
      API, and the embeddable widget. Bearer-token authenticated.
    contact:
      name: Unisson
      email: contact@unisson.ai
      url: https://www.unisson.ai/
    x-apievangelist-harvested: '2026-07-21'
- target: $
  update:
    servers:
    - url: https://api.unisson.ai
      description: Production (the published document declares no servers[])
- target: $.info
  update:
    x-apievangelist-notes: >-
      The live document at https://api.unisson.ai/openapi.json is a FastAPI
      export - operation summaries are present, but descriptions, tags
      metadata, examples, and 4xx responses beyond 422 validation errors are
      sparse. Errors use the FastAPI HTTPValidationError envelope rather than
      RFC 9457 problem+json.
